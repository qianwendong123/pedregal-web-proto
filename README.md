
  # Build Store Page Screen

  This is a code bundle for Build Store Page Screen. The original project is available at https://www.figma.com/design/6apyi7xzeYokjSrdzCx0kT/Build-Store-Page-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  